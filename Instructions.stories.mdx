import { Meta } from '@storybook/addon-docs';

<Meta title='Introduction' />

# Create React App

yarn create react-app signupform --template typescript

Remove content of app.tsx
Delete logo.svg

# Add Storybook

npx storybook init

Remove autogenerated folder, stories

# Styles

## Add TailwindCSS

npx tailwindcss init

`content: ["./src/**/*.{js,jsx,ts,tsx}", "./public/index.html"],`

Remove App.css and index.css
Create folder for styles
Add new file called tailwind.css
Add scripts for package.json

Add `staticDirs: ["../public"],` to main.js
Remove `-s public` from scripts

Add styles to storybook preview
`import "../src/styles/tailwind.output.css";`

# Add DaisyUI

yarn add daisyui

<!-- plugins: [require("daisyui")], -->

<div class='mockup-code'>
  <pre data-prefix='$'>
    <code></code>
  </pre>
</div>
